spring.application.name=backend

# ── Datasource (env-overridable; defaults for local development) ──
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/oct_platform}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Ww123321}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=9000
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
spring.mvc.async.request-timeout=60000

# ── JWT ──
jwt.secret=${JWT_SECRET:OCTPlatform2026-SecureJWTSecret-HMACKey-MedicalImaging-@#$%!}

# ── Gemini (AI chatbot) ──
gemini.api.key=${GEMINI_API_KEY:AQ.Ab8RN6Id42PGGta07S1LAHzWTZiV3-IcBXoUMreKkyzkv6MjRA}

# ── Mail (OTP password reset) ──
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${MAIL_USERNAME:mahmoodsheyab611@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ickf kmjq gzyz zvjg}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000
